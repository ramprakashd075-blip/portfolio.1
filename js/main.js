window.addEventListener('load',()=>document.querySelector('.loader').classList.add('hide'));
const cursor=document.querySelector('.cursor');document.addEventListener('mousemove',e=>{cursor.style.left=e.clientX+'px';cursor.style.top=e.clientY+'px'});
const reveals=document.querySelectorAll('.reveal');const io=new IntersectionObserver(entries=>{entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('active')})},{threshold:.15});reveals.forEach(r=>io.observe(r));
const counters=document.querySelectorAll('[data-count]');const countIO=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(!entry.isIntersecting)return;const el=entry.target,end=+el.dataset.count;let current=0;const step=Math.max(1,Math.ceil(end/60));const timer=setInterval(()=>{current+=step;if(current>=end){current=end;clearInterval(timer)}el.textContent=current},25);countIO.unobserve(el)})});counters.forEach(c=>countIO.observe(c));
document.querySelectorAll('.card,.project').forEach(card=>{card.addEventListener('mousemove',e=>{const r=card.getBoundingClientRect();card.style.transform=`perspective(800px) rotateX(${-(e.clientY-r.top-r.height/2)/18}deg) rotateY(${(e.clientX-r.left-r.width/2)/18}deg) translateY(-10px)`});card.addEventListener('mouseleave',()=>card.style.transform='')});
